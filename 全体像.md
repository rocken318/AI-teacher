# AI先生 ── 全体像

> 小1〜高3を貫く、**答えを教えず考えを引き出す** 安全な生成AI探究チューター。
> 最終形から逆算し、「安全に動く土台 → 対話の心臓部 → 学年展開 → 見守り → フルレンジ」の順で作る。

最終更新: 2026-08-26 / ステータス: 設計確定（Phase 0 着手前）

---

## 0. このドキュメントの位置づけ

- これは **設計の一枚岩（マスタードキュメント）**。全員がここを見れば全体像が分かる。
- 実装の起点は **Phase 0 の「一発目のプロンプト」**（別ファイル `一発目のプロンプト.md`）。
- 前提となる市場理解は **競合調査レポート**（リポジトリ `index.html` = `小学生向けAI学習 競合調査レポート`）。

---

## 1. ビジョン

**「子どもが、AIに答えを教わるのではなく、AIとの対話で自分の頭で答えにたどり着く」** 体験をつくる。

- 対象：**小1〜高3**（K-12フルレンジ）。ただし1つのエンジンで学年を出し分ける。
- 看板：**探究・思考力**。身近な問い（例：「なぜ空は青いの？」）を教科横断で深める。
- 核の作法：**ソクラテス型**。答えを直接与えず、「なぜ？」「どう考えた？」で引き出す。
- 譲れない軸：**安全性**。子どもに安心して使わせられる設計（後述）。

---

## 2. なぜ勝てるか（差別化）

競合調査（`index.html`）で判明した空白を突く。

| 競合の状況 | AI先生の取り方 |
|---|---|
| 大半は**ルールベースのアダプティブ出題**（真の生成AI対話ではない） | 生成AI対話をコアに据える |
| 生成AIコーチは**小4以上が中心**（低学年は手薄） | **低学年(小1〜3)も安全に**カバー |
| 大手のAIコーチは**既存会員限定・単体販売なし** | **独立プロダクト**として成立させる |
| 既存は**教科ドリル最適化**が中心 | **探究・思考力・教科横断**を看板に |
| 子ども向け生成AIは**安全性が未成熟**（ハルシネーション/年齢/同意） | **安全性・規制対応**を強みに変える |

**一言でいう差別化：** 「安心して低学年から使わせられる、答えを教えず考えを育てる探究AI」。

---

## 3. 技術スタック（案A・確定）

目的（生成AI対話・子どもの安全・学習履歴・保護者見守り・小1〜高3）が要求するのは
**バックエンド必須／DB必須／認証必須／モデレーション必須** の構成。静的HTMLでは安全要件を満たせない。

| 領域 | 採用 | 理由 |
|---|---|---|
| 言語 / FW | **TypeScript + Next.js (App Router)** | フロント〜APIを1言語。反復が最速。Claude Codeとの相性最良 |
| LLM | **Claude**（`claude-opus-4-8` / `claude-sonnet-4-6` / `claude-haiku-4-5`） | 子ども向けの安全挙動が最強。用途別にモデルを出し分けコスト最適化 |
| DB / 認証 / ストレージ | **Supabase**（PostgreSQL + Auth + Storage + RLS） | 行レベルセキュリティが「本人と保護者しか見えない」要件に直結。MVP→本番を同一基盤で |
| ORM | **Drizzle ORM** | TSネイティブで軽量・型安全 |
| スタイリング | **Tailwind CSS** | 年齢適応UIを素早く |
| ホスティング | **Vercel** | Next.jsと一体。プレビュー環境が自動 |
| 進捗管理 | **GitHub** | ソース・進捗管理のみ（ホスティングはVercel） |

**モデルの出し分け方針**
- `haiku` … 入出力モデレーション・分類・軽い判定（安く速く）
- `sonnet` … 通常の探究対話（主力）
- `opus` … 難度の高い対話・中高生・複雑な探究の深掘り

---

## 4. アーキテクチャ

### 4.1 安全パイプライン（全対話が必ず通る）

```
子どもの発話
   ↓
[入力モデレーション]  ← haiku：不適切/危険/個人情報を検知
   ↓
[ソクラテス対話エンジン]  ← sonnet/opus：学年プロファイルを注入して応答生成
   ↓
[出力モデレーション]  ← haiku：ハルシネーション兆候/不適切表現/「答え直答」を抑止
   ↓
[会話ログ保存]  ← Supabase(RLS)：本人と保護者のみ閲覧可
   ↓
子どもへ返答
```

この端から端までの一本を **Phase 0 で最小構成で通す**（walking skeleton）。

### 4.2 学年パラメータ化（1エンジンで小1〜高3）

対話エンジンは学年に依存しない。学年ごとの違いは **学年プロファイル** としてデータで注入する。

```
GradeProfile {
  gradeBand      // 例: 小1-3 / 小4-6 / 中 / 高
  vocabularyLevel// 語彙のやさしさ（ひらがな寄り〜抽象語OK）
  sentenceLength // 一文の長さ・情報量
  topics         // 扱える探究テーマ集合
  strictness     // どこまで自力に委ねるか（足場の量）
  safetyLevel    // モデレーションの厳しさ
  modality       // text / voice補助（低学年は音声寄り）
}
```

→ 新しい学年の追加は「プロファイルを足す」だけ。エンジンのコードは変えない。

### 4.3 ロール

- **子ども**：対話する本人
- **保護者**：見守り（会話ログ・探究の軌跡・つまずきを閲覧）
- （将来）**先生**：塾・学校導入時のクラス管理

RLSで「子どものデータは本人と紐づく保護者だけが見える」を強制。

---

## 5. データモデル（初期スケッチ）

```
User        (id, role: child|guardian, ...)
Guardianship(guardian_id, child_id)          // 保護者⇄子ども
GradeProfile(id, gradeBand, params...)        // 学年プロファイル（4.2）
Session     (id, child_id, topic, grade_profile_id, started_at)
Message     (id, session_id, sender: child|ai, text, created_at)
Moderation  (id, message_id, stage: in|out, verdict, reason)  // 安全ログ
Insight     (id, session_id, kind: 気づき|つまずき, summary)   // 見守り用要約
```

---

## 6. ロードマップ（最適な構築順）

### Phase 0 ── 環境ブートストラップ ★一発目のプロンプトが担当
最適スタック(案A)で骨格を立ち上げ、**1つだけ動く探究対話**を端から端まで通す（walking skeleton）。
安全パイプライン（入力モデ→対話→出力モデ→ログ）が実際に通ることを証明する。
- 成果物：動くプロジェクト骨格、DBスキーマ雛形、安全パイプラインの最小実装、1テーマの対話画面

### Phase 1 ── ソクラテス型対話エンジン
「答えを教えず、なぜ？どう考えた？で引き出す」中核ロジックを、学年プロファイルで出し分け可能に。
探究テーマを数本搭載。エンジンの品質をここで作り込む。

### Phase 2 ── 学年展開：小1〜6（`小学生編` の実体化）
低学年（やさしい語彙・短文・ひらがな寄り・音声補助）〜高学年をプロファイルでカバー。

### Phase 3 ── 保護者の見守り＆学習履歴
会話ログ・探究の軌跡・つまずきを保護者ダッシュボードで可視化。RLSで安全に。
→ ここで「安心して子どもに使わせられる」が製品として立つ。

### Phase 4 ── 中学・高校へ拡張（小1〜高3フルレンジ）
学年プロファイルを中1〜高3まで拡張。難度・題材を上げ、探究×教科横断を深化。モデルはOpus寄りに。

### Phase 5 ── 磨き込み
音声、UIの年齢適応、コスト最適化（haiku/sonnet/opusの出し分け）、効果検証。

---

## 7. 安全設計（子ども向け生成AIの必須要件）

- **入出力の二段モデレーション**：危険・不適切・個人情報・直答を抑止。
- **答えを直接教えない原則**：出力モデレーションで「答え直答」を検知し、問い返しに変換。
- **全会話ログ**：保護者が後から確認できる。RLSで本人＋保護者に限定。
- **年齢適応**：学年プロファイルで安全レベル・語彙・題材を制御。
- **エスカレーション**：AIが安全に扱えない話題は対話を止め、保護者に知らせる導線（将来、進研ゼミの人的エスカレーションに相当）。
- **文科省ガイドライン準拠**を設計思想の前提に置く。

---

## 8. 成果物一覧（このブレストのアウトプット）

1. **`全体像.md`**（本書）── 設計の一枚岩
2. **`一発目のプロンプト.md`** ── Phase 0 をClaude Codeに作らせる最初の指示
3. **LP（ランディングページ）** ── 仕組みを分かりやすく伝える

---

## 9. 未決事項（実装前に詰める）

- 音声（低学年）の実装をPhase 0/2どちらで入れるか
- 保護者アカウントと子どもアカウントの作成フロー（誰が先に登録するか）
- 料金モデル（単体販売の価格・無料枠）※製品化フェーズで
- 効果検証の指標（探究の深まりをどう測るか）
