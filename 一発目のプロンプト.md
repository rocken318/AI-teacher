# 一発目のプロンプト（Phase 0：環境ブートストラップ）

> このファイルの下の「▼ここからコピー」〜「▲ここまで」を、そのまま Claude Code に貼り付けて実行する。
> 目的：最適スタック（案A）でプロジェクト骨格を立ち上げ、**1つだけ動く探究対話**を
> 安全パイプライン込みで端から端まで通す（walking skeleton）。全機能は作らない。

参照：設計の全体像は `全体像.md`。市場前提は `index.html`（競合調査レポート）。

---

## このプロンプトの狙い（人間向けメモ）

- **やること**：土台＋薄い一本。安全パイプライン（入力モデ→対話→出力モデ→ログ）が実際に通ることの証明。
- **やらないこと**：学年フル展開、保護者ダッシュボード、音声、認証の作り込み、複数テーマ。→ Phase 1以降。
- **完了の定義**：ローカルで起動でき、子ども役が1テーマで対話でき、答えを直答せず問い返し、会話がDBに残る。

---

## ▼ここからコピー（Claude Codeへ）

あなたは「AI先生」というプロダクトの Phase 0（環境ブートストラップ）を実装します。
**全機能を作らないでください。** 目的は「最適な土台」と「1本だけ端から端まで動く探究対話（walking skeleton）」です。

### プロダクトの目的
小1〜高3を対象に、**答えを直接教えず、問い返しで子ども自身に考えさせる**（ソクラテス型）安全な生成AI探究チューター。
最終的に小1〜高3をカバーするが、**今回は最小の1スライスだけ**を動かす。

### 採用スタック（変更しないこと）
- TypeScript + Next.js（App Router）
- LLM：Claude（`claude-haiku-4-5`＝モデレーション用、`claude-sonnet-4-6`＝対話用）。Anthropic SDK を使用。APIキーは `.env.local` の `ANTHROPIC_API_KEY` から読む。**キーはサーバー側だけで使い、クライアントに絶対露出しない。**
- DB：PostgreSQL（Supabase 前提）。ORM は Drizzle。**今回は接続情報が無くても動くよう、DBが未設定ならローカルのSQLite等にフォールバックしてよい**（会話ログが残ることが目的）。
- スタイリング：Tailwind CSS
- 進捗管理：Git（`git init` し、意味のある単位でコミット）

### 実装スコープ（これだけ作る）
1. **プロジェクト骨格**：Next.js + TypeScript + Tailwind + Drizzle をセットアップ。`README.md` に起動手順を書く。
2. **安全パイプライン（最小実装）**：1回の対話が必ず次を通る。
   - `[入力モデレーション]` haiku で、危険・不適切・個人情報を判定（JSONで `{ ok, reason }` を返させる）。NGなら対話に進まず、やさしい定型文で切り返す。
   - `[対話生成]` sonnet で、**ソクラテス型システムプロンプト**（下記）に沿って応答。
   - `[出力モデレーション]` haiku で、「答えを直答していないか／不適切表現がないか」を判定。直答していたら**問い返しに変換**して返す。
   - `[ログ保存]` セッションと各メッセージ、各モデレーション結果をDBに保存。
3. **対話画面**：1テーマ「なぜ空は青いの？」だけ。チャットUI。子どもが入力→AIが問い返す。画面下に「これは Phase 0 の walking skeleton」と明記。
4. **学年パラメータの型だけ用意**：`GradeProfile`（gradeBand, vocabularyLevel, sentenceLength, strictness, safetyLevel）を型・データとして定義し、今回は「小1-3」固定で対話に注入する。**エンジンは学年に依存しない構造**にする（後で学年を足すだけで拡張できるように）。

### ソクラテス型システムプロンプト（対話生成に使う中身。日本語で子どもに話す）
- あなたは小学校低学年の子どもの探究をサポートする「AI先生」。
- **絶対に答えを直接言わない。** 代わりに「どうしてそう思う？」「どこで見たことある？」など、子どもが自分で気づける問いを1つずつ返す。
- ひらがな多め・短い文・やさしい言葉。1回の返答は2〜3文まで。
- 子どもの発言を必ず一度受け止めてから問い返す（否定しない）。
- 個人情報（名前・住所・学校名など）は聞かない。言われても保存や深掘りをしない。
- 危険・不適切な話題になったら、そっと話題を学びに戻す。

### 完了条件（この状態になったら停止し、報告する）
- `npm run dev` でローカル起動できる。
- ブラウザで「なぜ空は青いの？」の対話ができ、AIが**答えを直答せず問い返す**。
- 入力/出力モデレーションが実際に走り、結果がログに残る（DBまたはフォールバック先）。
- `ANTHROPIC_API_KEY` が未設定でも、その旨を画面に表示してクラッシュしない。
- `README.md` に「セットアップ」「起動」「環境変数」「今回作った範囲／作っていない範囲」を記載。
- 変更を Git にコミット。

### 進め方
1. 最初に **実装計画を箇条書きで提示**し、フォルダ構成とファイル一覧を見せる。
2. 承認を待たずに骨格から着手してよいが、**安全パイプラインの各段は独立した関数**に分け、後から差し替え可能にする。
3. 迷ったら「Phase 0 の最小」に寄せる。**スコープを広げない。**
4. 最後に、動作確認手順と、次の Phase 1（ソクラテス対話エンジンの本作り込み）への申し送りを書く。

## ▲ここまで

---

## 実行後のチェックリスト（人間向け）

- [ ] `npm run dev` で起動できたか
- [ ] 「なぜ空は青いの？」で **答えを直答せず問い返して**きたか
- [ ] 入力/出力モデレーションのログが残っているか
- [ ] APIキー未設定でもクラッシュしないか
- [ ] `README.md` に「作った範囲／作っていない範囲」が書かれているか
- [ ] Git にコミットされているか

→ すべてOKなら Phase 1（`全体像.md` の6章）へ。
