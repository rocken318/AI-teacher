# Phase 1 のプロンプト（ソクラテス対話エンジンの本作り込み）

> 下の「▼ここからコピー」〜「▲ここまで」を Claude Code に貼って実行する。
> 目的：Phase 0 の walking skeleton を、**質の高いソクラテス対話エンジン**に育てる。
> テーマを複数化し、学年プロファイルの出し分けを"実際に効く"状態にする。

参照：`全体像.md`（6章 Phase 1）/ Phase 0 の実装（`src/lib/**`）。

---

## このフェーズの狙い（人間向けメモ）

- **やること**：①対話の質（足場かけ・ヒント段階化・「わかった！」の承認）②探究テーマを複数③学年プロファイルを実際に切替可能に④エンジンの振る舞いを守るテスト。
- **やらないこと**：保護者ダッシュボード(Phase 3)、認証の作り込み、音声、中高フル展開(Phase 4)。**小1-3と小4-6の2バンドで"効く"ことを証明**できれば十分。
- **前提**：本物の対話品質を見るため `.env.local` に `ANTHROPIC_API_KEY` を入れてから検証する。

---

## ▼ここからコピー（Claude Codeへ）

あなたは「AI先生」の Phase 1 を実装します。Phase 0 の土台（Next.js + TS + 安全パイプライン）は完成済みです。
**土台の構造（安全パイプラインの4段・学年非依存エンジン）は壊さないでください。** その中身の"質"を上げるのが今回の仕事です。

まず `全体像.md` の6章(Phase 1)と、既存の `src/lib/prompts.ts` / `src/lib/grade/gradeProfiles.ts` / `src/lib/safety/*` / `src/app/api/chat/route.ts` / `src/components/Chat.tsx` を読んでから始めてください。

### スコープ（この4つを作る）

1. **ソクラテス対話エンジンの質を上げる**（`src/lib/prompts.ts` と生成まわり中心）
   - **足場かけ（scaffolding）**：子どもが詰まったら、いきなり答えず「ヒントの段階」を上げる。
     例：漠然とした問い → 選択肢を出す問い → 身近な例に引き寄せる問い、と段階化する。
   - **同じ問いを繰り返さない**：直近のやりとりを踏まえ、毎回"一歩進む"問いにする。
   - **「わかった！」の承認**：子どもが自力で答えに近づいたら、答えを言わずに気づきを認めて次の探究へ橋渡しする。
   - **1返答＝受け止め1＋問い1** の型は維持。学年プロファイルで語彙・文長・足場の量(strictness)を必ず反映する。
   - これらを実現するため、生成に渡すシステムプロンプトを強化し、必要なら「対話の状態（詰まり度・ステップ）」を軽く推定する補助を入れてよい（ただし過剰設計にしない）。

2. **探究テーマを複数化**
   - Phase 0 のハードコード1テーマ（"なぜ空は青いの？"）をやめ、**テーマ集**を作る（例：なぜ空は青い／氷はなぜ水になる／なぜ月は形が変わる／どうして虹は7色？ など5本前後）。
   - テーマは `src/lib/topics.ts` などにデータとして定義（`{ id, title, gradeBands, seedQuestion }`）。学年で出し分けできる形に。
   - **トップ画面でテーマを選んでから対話に入る** UI にする（`src/app/page.tsx` / `Chat.tsx`）。API(`/api/chat`)は `topicId` を受け取り、セッションに紐付けて保存する。

3. **学年プロファイルの出し分けを"実際に効く"状態に**
   - 画面で**学年帯を選べる**ようにする（最低 `小1-3` と `小4-6` の2つ）。選んだ profile を API に渡し、対話・モデレーションに反映する。
   - 同じテーマでも、小1-3（ひらがな・とても短い・足場多め）と小4-6（やさしい漢字・短い・足場中）で**明確に応答が変わる**ことを確認できるようにする。

4. **エンジンの振る舞いを守るテスト**
   - テストランナーを導入（Vitest 推奨）。`npm test` で走るように。
   - 最低限、次を検証する：
     - `buildSocraticSystemPrompt(profile)` が profile の語彙・文長・strictness を反映している（文字列アサーション）。
     - `moderateOutput` の判定パーサ（`parseOutputVerdict`）が「directAnswer=true のJSON」を正しく検知する。
     - トピックデータの整合（各 topic に seedQuestion があり、gradeBands が有効値）。
   - APIキーに依存する実LLM呼び出しはテストで叩かない（モック or 純関数のみ対象）。

### 制約・原則
- 安全パイプライン（入力モデ→対話→出力モデ→ログ）の**4段は必ず維持**。テーマ・学年が増えても全発話が4段を通ること。
- `ANTHROPIC_API_KEY` 未設定でも起動・操作できる「練習モード」を維持（テーマ選択・学年選択は動く）。
- DBは Phase 0 の SQLite フォールバックのまま。スキーマに `topicId` 等を足す場合は Drizzle のマイグレーションで。
- **スコープを超えない**（保護者機能・認証・音声・中高展開は入れない）。

### 進め方
1. 最初に**実装計画とファイル差分の一覧**を提示する。
2. `prompts.ts` の強化 → `topics.ts` 追加 → UI（テーマ/学年選択）→ API 接続 → テスト、の順で進める。
3. 途中で `npm run build` と `npm test` を通しながら進める。
4. 完了したら **動作確認手順**（キー有り/無し両方）と、**Phase 2（小1〜6フル展開）への申し送り**を README 末尾に追記し、git コミットする。

### 完了条件（この状態で停止・報告）
- トップでテーマと学年帯を選び、対話に入れる。
- 同一テーマで小1-3と小4-6の応答が明確に違う（語彙・文長・足場）。
- 対話が詰まったとき、答えを言わずヒントの段階が上がる（実キーで確認）。
- 全発話が安全4段を通り、`topicId`・学年がログに残る。
- `npm run build` と `npm test` が通る。
- README 更新＋git コミット。

## ▲ここまで

---

## 実行後チェックリスト（人間向け）
- [ ] テーマ選択→学年選択→対話、の導線が動くか
- [ ] 小1-3 と 小4-6 で応答が体感で変わるか
- [ ] 詰まったとき直答せずヒントが段階的に出るか（要APIキー）
- [ ] `topicId`・学年がDBログに残るか
- [ ] `npm run build` / `npm test` 通過
- [ ] README申し送り＋コミット

→ OKなら Phase 2（`全体像.md` 6章：小1〜6のフル展開・低学年の音声補助検討）へ。
